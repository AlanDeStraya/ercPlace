import React from 'react';

const ScadaLive = ({ socket }) => {

	return (
		<div id='scada-live'>
			<button>Train</button>
			<button>Infrastructure</button>
			<button>Other</button>
		</div>
	);

};

export default ScadaLive;
