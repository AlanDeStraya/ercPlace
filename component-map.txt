index
	app ##socket $state(darkness, openTab, siteUpdatesPopupActive, diversionPageOpen, isConnected)
		header &props(socket, darkness, openTab)

		?alert

		/sharepoint
			weather ?radar
			sidebar
				?fire
				?obstruction
			print popup
			scada popup
			updates popup

		/Diversion $state(diversionActive, obstructionPlanNumber) &props(socket)

			ObstructionFinder $state(numTrains, numTrainsDeclared, selectedAreas, openBoxes) &props(obstructionPlanNumber)
				FinderControl &props(numTrains, numTrainsDeclared, obstructionPlanNumber, openBoxes)
				?AreaSelector &props(obstructionPlanNumber, openBoxes, selectedAreas)
				?PlanDetails &props(obstructionPlanNumber, openBoxes)

			DiversionControl $state(eventStartTime) &props(socket)
			DiversionLive &props(diversionActive, obstructionPlanNumber, socket)
				CommsLive &props(socket)
					Chat &props(socket)
				MainlineLive
					suggestions?
				ScadaLive

			?log

		/ios finder

		?footer
