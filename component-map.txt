index
	app
		header
		
		?alert
		
		/sharepoint
			weather ?radar
			sidebar
				?fire
				?obstruction				
			print popup
			scada popup
			updates popup
			
		/diversion ##socket
			/divApp $state(divActive, plan, timeElapsed, log#)
				
				obstructionFinder $state(areas, openBoxes, numTrains) &props(plan)
					finder control &props(numTrains, plan)
					?area select &props(areas, openBoxes, plan)
					?details &props(openBoxes, plan)
					
				diversion control &props(timeElapsed)
				diversion main &props(divActive, plan)
					comms
						chat
					main
						suggestions?
					SCADA
				
				chat area
				
			/log
			
		/ios finder
		
		?footer